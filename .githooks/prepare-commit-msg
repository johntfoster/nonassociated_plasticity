#!/bin/sh
set -eu
case "${2:-}" in merge|squash|commit) exit 0 ;; esac
if grep -Eq '^(##[[:space:]]+)?Summary[[:space:]]*$' "$1"; then exit 0; fi
cat >> "$1" <<'LOG'

Summary

What changed & why

Alternatives considered

Dead ends & backtracks

Open questions

Next steps
LOG
